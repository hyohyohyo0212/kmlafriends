rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 테스트/시연용 규칙입니다.
    // 익명 로그인한 사용자만 읽기/쓰기가 가능합니다.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
